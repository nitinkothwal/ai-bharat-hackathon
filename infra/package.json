{
    "name": "bharat-care-link-infra",
    "version": "1.0.0",
    "scripts": {
        "build": "node build.js",
        "deploy": "bash deploy.sh",
        "deploy:infra": "bash deploy.sh --skip-webapp",
        "deploy:full": "bash deploy.sh"
    }
}